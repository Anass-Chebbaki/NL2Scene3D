ROOM: {scene_name}
Safe bounds (wall margin included): X [{x_min:.3f}, {x_max:.3f}] m | Y [{y_min:.3f}, {y_max:.3f}] m
Dimensions: {room_width:.2f} m (W) × {room_depth:.2f} m (D) × {room_height:.2f} m (H)
Objects to place: {n_roots}

═══════════════════════════════════════════════════════
STRUCTURAL ELEMENTS (Fixed — do NOT move or output these)
═══════════════════════════════════════════════════════
###STRUCTURAL_JSON###

═══════════════════════════════════════════════════════
ROOT MOVABLE OBJECTS to reorganize
═══════════════════════════════════════════════════════
###FLAT_JSON###

═══════════════════════════════════════════════════════
INSTRUCTIONS
═══════════════════════════════════════════════════════
The current positions are DISORGANIZED. Ignore them completely.
Design a new, professional layout from scratch following the system prompt rules.

For each object:
1. Identify its role from "name" and "category".
2. Choose a wall/position following the semantic rules.
3. Compute its new AABB using the formula in the system prompt.
4. Verify it fits inside the safe bounds and does not overlap any other object.
5. Include it in the output with exact "name", "x", "y", "rz_deg".

Remember:
- Children listed in "children" fields are moved automatically — do NOT output them.
- rz_deg MUST be exactly 0, 90, 180, or 270.
- x, y are the geometric CENTER of the object.
- Use the aabb_2d formula: for rz_deg=0/180 → eff_w=w, eff_d=d; for rz_deg=90/270 → eff_w=d, eff_d=w.

═══════════════════════════════════════════════════════
OUTPUT (JSON array only — no other text)
═══════════════════════════════════════════════════════