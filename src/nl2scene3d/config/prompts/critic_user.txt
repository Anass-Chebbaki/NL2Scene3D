You are reviewing a 3D room scene that has been reorganized by an AI system.
You are given MULTIPLE VIEWS of the same scene to help you understand the spatial layout:

IMAGE 1 (Top-Down Orthographic): Shows the room from directly above. Use this view to verify:
- Correct XY positions (no overlapping footprints)
- Proper spacing between objects
- Objects within room boundaries
- Functional groupings (desk near chair, nightstand near bed, etc.)

IMAGE 2 (Isometric View - Primary Angle): Shows the room from an elevated diagonal angle. Use this to verify:
- Objects appear naturally placed (not floating, not clipping)
- Rotations look correct for each object type
- Visual balance of the room

IMAGE 3 (Isometric View - Opposite Angle): Shows the OPPOSITE side of the room. Use this to verify:
- Objects hidden in the primary view are correctly placed
- The back of the room is not cluttered or empty

IMAGE 4 (Front Low View): Shows the room from a low, near-horizontal angle. Use this to verify:
- Objects are resting on the floor (Z height is correct)
- Nothing is floating unreasonably high
- Wall-mounted objects are at appropriate heights

Please analyze ALL VIEWS together and identify any visual imperfections or
suboptimal placements. Focus on:
1. Objects whose FOOTPRINTS overlap when seen from top-down view
2. Objects that seem to be floating or clipping into other objects
3. Rotations that look unnatural for the object type (e.g. a desk facing a wall with no space to sit)
4. Objects that are inside or too close to walls
5. Furniture groups that could be better arranged functionally

IMPORTANT GUIDELINES:
- Use the TOP-DOWN view as your PRIMARY reference for positions and overlaps.
  The isometric views can create optical illusions about collisions.
- Wall-mounted objects (shelves, lamps, AC units) at high Z will visually overlap
  with floor furniture in isometric views. This is NORMAL. DO NOT move them.
- Only suggest corrections you are HIGHLY CONFIDENT about from multiple views.
- Prefer SMALL adjustments (< 0.5m) over large repositioning.
- If the layout looks generally good, give it a HIGH SCORE (8+) and return
  an EMPTY corrections list. DO NOT make corrections just for the sake of it.

Return a JSON object with the following structure:
{{
  "quality_assessment": "brief overall assessment of the current layout",
  "score": <integer from 1 to 10, where 10 is perfect>,
  "corrections": [
    {{
      "object_name": "<exact object name from the scene>",
      "action": "<move|rotate|move_and_rotate>",
      "new_location": [x, y, z],
      "new_rotation_euler": [rx, ry, rz],
      "reason": "<brief explanation referencing which VIEW revealed the issue>"
    }}
  ]
}}

If the layout looks good and no corrections are needed, return:
{{
  "quality_assessment": "Layout is well organized",
  "score": <score>,
  "corrections": []
}}

Current room bounds: X [{x_min:.2f}, {x_max:.2f}], Y [{y_min:.2f}, {y_max:.2f}]
Units: METERS. Coordinate System: Right-handed (+X: East, +Y: North, +Z: Up).

IMPORTANT: Return ONLY valid JSON. Do not include any other text.