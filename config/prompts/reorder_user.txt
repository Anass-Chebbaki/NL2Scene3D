ROOM INFORMATION:
- Scene name: {scene_name}
- Room bounds: X [{x_min:.2f}, {x_max:.2f}], Y [{y_min:.2f}, {y_max:.2f}]
- Room dimensions: {room_width:.2f}m x {room_depth:.2f}m x {room_height:.2f}m

DISORGANIZED SCENE STATE:
The following JSON describes the current disorganized state of the room.
Please reorganize the movable objects (is_movable: true) following the
interior design principles provided in the system prompt.

{scene_json}

IMPORTANT REMINDERS AND GEOMETRIC CONSTRAINTS:
- You MUST carefully read the "dimensions" array [width, depth, height] for each object.
- For large furniture (bed, desk, shelf), MENTALLY CALCULATE their physical footprint. Ensure the distance between large furniture items is greater than the sum of half their widths/depths to avoid collisions.
- EXCEPTION FOR DECORATIONS/SMALL ITEMS: Small objects (monitor, keyboard, books, bottle, lamp) MUST be placed at the EXACT SAME X and Y coordinates as their logical parent furniture (e.g. monitor on desk, books on shelf, lamp on nightstand). They SHOULD overlap in X and Y because they sit on top of the furniture in the Z axis.
- Keep all Z coordinates unchanged. Only modify X and Y.
- Stay strictly within the room bounds: X [{x_min:.2f}, {x_max:.2f}], Y [{y_min:.2f}, {y_max:.2f}].
- Return a complete JSON with ALL objects (both movable and non-movable).
- For non-movable objects, keep their original location and rotation_euler perfectly unchanged.